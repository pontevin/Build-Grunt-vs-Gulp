{
	"name": "grunt-vs-gulp",
	"version": "1.0.1",
	"devDependencies": {
		"grunt": "^1.1.0",
		"grunt-contrib-concat": "^1.0.1",
		"grunt-contrib-cssmin": "^3.0.0",
		"grunt-contrib-imagemin": "^4.0.0",
		"grunt-contrib-uglify": "^4.0.1",
		"grunt-sass": "^3.1.0",
		"gulp": "^4.0.2",
		"gulp-clean-css": "^4.3.0",
		"gulp-concat": "^2.6.1",
		"gulp-imagemin": "^7.1.0",
		"gulp-sass": "^4.1.0",
		"gulp-stats": "0.0.4",
		"gulp-uglify": "^3.0.2",
		"node-sass": "^4.14.1",
		"npm-run-all": "^4.1.5",
		"time-grunt": "^2.0.0"
	},
	"scripts": {
		"clean:grunt": "if [ -d 'dist-grunt' ]; then rm -Rf dist-grunt; fi",
		"build:grunt": "grunt",
		"clean:gulp": "if [ -d 'dist-gulp' ]; then rm -Rf dist-gulp; fi",
		"build:gulp": "gulp",
		"clean": "npm-run-all clean:*",
		"build": "npm-run-all build:*"
	}
}
